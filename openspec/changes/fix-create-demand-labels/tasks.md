# Tasks - Fix Create Demand Labels

- [ ] **1. Localization**
    - [ ] 1.1 Add `work.demandDescription` to `src/locales/pt/translation.json` ("Descreva a demanda (o que precisa ser feito)").
    - [ ] 1.2 Add `work.demandDescription` to `src/locales/en/translation.json` ("Describe the demand (what needs to be done)").

- [ ] **2. UI Updates**
    - [ ] 2.1 Update `CreateDemand.tsx` to use `t('work.demandDescription')`.

- [ ] **3. Verification**
    - [ ] 3.1 Verify that the helper text is correct in the Create Demand form.
