# Tasks: Kanban Mobile Layout

- [x] 1. Ajustar estilo dos filtros e botões de cabeçalho no quadro Kanban para evitar corte (habilitar `flex-wrap` ou ajustar as larguras).
- [x] 2. Atualizar o contêiner principal do Kanban para suportar rolagem horizontal (`overflow-x: auto`, `flex-wrap: nowrap` ou grid com suporte a scroll).
- [x] 3. Garantir que as colunas do Kanban (`KanbanColumn`) tenham uma largura mínima definida (`min-width` e `flex-shrink: 0`) nas visualizações mobile para que não se espremam em uma única tela.
- [x] 4. Testar o layout em uma visualização móvel (por ex. 375px de largura) para validar o comportamento dos filtros, botões e scroll horizontal das colunas.
