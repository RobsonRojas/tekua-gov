# Tasks - Improve Language Selector Icon

- [x] **1. Component Refactoring**
    - [x] 1.1 Locate `LanguageSelector` (likely in `src/components/LanguageSelector.tsx`).
    - [x] 1.2 Update the toggle button to display the current language code (e.g., "PT" or "EN") next to the icon.
    - [x] 1.3 Add a subtle background or border to the button to make it look more like a selector and less like a generic action button.

- [x] **2. Verification**
    - [x] 2.1 Verify that the button updates immediately when the language is changed.
    - [x] 2.2 Verify the UI alignment across different screen sizes.
