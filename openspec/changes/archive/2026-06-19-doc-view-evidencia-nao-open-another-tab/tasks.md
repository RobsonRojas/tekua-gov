## 1. Components

- [x] 1.1 Create `EvidenceViewerModal` component that takes an evidence URL and displays it in a dialog.

## 2. Integration

- [x] 2.1 Update `src/pages/TaskDetail.tsx` to include the `EvidenceViewerModal` and manage its state.
- [x] 2.2 Replace `window.open` in `TaskDetail.tsx` with opening the modal.
- [x] 2.3 Update `src/components/ActivityCard.tsx` to include the `EvidenceViewerModal` and manage its state.
- [x] 2.4 Replace `target="_blank"` link in `ActivityCard.tsx` with opening the modal.

## 3. Verification

- [x] 3.1 Test evidence viewing in `TaskDetail` and `ActivityCard` to ensure the modal opens correctly and the image is visible.
