## 1. Backend Implementation

- [x] 1.1 Update `confirm_activity` database RPC to allow execution if the user is an administrator or the requester.

## 2. Frontend Implementation

- [x] 2.1 Identify the Work Wall/Activity detail component that displays the "Confirm" action for `requester_approval` validation method.
- [x] 2.2 Update the UI condition to display the "Confirm" action for administrators in addition to the requester.
- [x] 2.3 Verify functionality for both requester and administrator personas.
