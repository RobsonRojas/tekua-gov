## 1. Backend API Edge Function Improvement

- [x] 1.1 Atualizar `supabase/functions/api-work/index.ts` sob a ação `moderateActivity` para utilizar o cliente de autenticação `supabaseClient.rpc` em vez do cliente admin `supabaseAdmin.rpc`.

## 2. Verification & Build

- [x] 2.1 Verificar que a compilação do projeto continue funcionando sem erros executando `npm run build`.
