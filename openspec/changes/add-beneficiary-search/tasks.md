# Tasks: Add Search to Beneficiary Selection

## 1. Frontend Implementation
- [x] Identify the file where the Beneficiary dropdown is rendered (e.g. `src/pages/NewWork.tsx` or similar).
- [x] Replace the standard MUI `<Select>` component with an MUI `<Autocomplete>` component.
- [x] Ensure the `<Autocomplete>` correctly displays the `full_name` of each member and updates the form state with the selected member's `id`.
- [x] Verify that the field handles "Outro Membro" (if it's a specific logic/option) properly, or ensure it seamlessly integrates with the existing fallback options.
