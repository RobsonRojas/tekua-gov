# Tasks - Improve Language Selector Icon

- [ ] **1. Component Refactoring**
    - [ ] 1.1 Locate `LanguageSelector` (likely in `src/components/LanguageSelector.tsx`).
    - [ ] 1.2 Update the toggle button to display the current language code (e.g., "PT" or "EN") next to the icon.
    - [ ] 1.3 Add a subtle background or border to the button to make it look more like a selector and less like a generic action button.

- [ ] **2. Verification**
    - [ ] 2.1 Verify that the button updates immediately when the language is changed.
    - [ ] 2.2 Verify the UI alignment across different screen sizes.
